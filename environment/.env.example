# Local application env overrides. Copy to environment/.env for local Go runs
# and Docker Compose env_file values.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=salestracker

POSTGRES_HOST=localhost
POSTGRES_PORT=28436
POSTGRES_NAME=salestracker
POSTGRES_SSLMODE=disable

SERVER_PORT=8080
SERVER_READ_TIMEOUT=10
SERVER_WRITE_TIMEOUT=10

APP_ENV=development
APP_LOG_LEVEL=debug

KAFKA_BROKERS=localhost:9094
KAFKA_TOPIC=transactions.events
KAFKA_DLQ_TOPIC=transactions.events.dlq
KAFKA_CONSUMER_GROUP_ID=diploma-transactions-consumer
CONSUMER_METRICS_PORT=2112
KAFKA_EXPORTER_PORT=9308

# Format: token:user_id:role[,token:user_id:role]
SECURITY_AUTH_TOKENS=replace-with-long-random-token:11111111-1111-1111-1111-111111111111:operator
MONITORING_LARGE_AMOUNT_THRESHOLD=100000
